@model geeks.Models.EventModel

<div class="well">
    @if (Model.Invitees != null && Model.Invitees.Any())
    {
        for(var i=0; i<Model.Invitees.Count(); i++)
        {
            @Html.HiddenFor(model => model.Invitees[i].UserId)
            <div>@Model.Invitees[i].Email</div>
        }
    }
</div>